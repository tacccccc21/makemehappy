<h2>参加中のイベント</h2>
    <% @like.each do |like| %>
      <% event = Tweet.find(like.tweet_id) %>
            <div class="name3"><%= link_to tweet, tweet_path(tweet) %></div>
            <div class="name2"><%= link_to "参加中のイベントから削除",like_path(like), method: :delete %></div>
            <div class="name2">
            
            </div>
            <div class="name2">
            
            </div>
    <br />
    <% end %>
</div>
